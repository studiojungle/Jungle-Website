    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@11/swiper-bundle.min.css">
        <!-- Font CDN options: uncomment and replace with your provider's URL if using a CDN -->
        <!-- Preconnect hints (Google Fonts shown as example) -->
        <!--
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        -->
        <!-- Example: Google Fonts (replace with actual family if available)
        <link href="https://fonts.googleapis.com/css2?family=Schibsted+Grotesk:wght@400;700&family=New+Kansas:wght@400;700&display=swap" rel="stylesheet">
        -->
        <!-- Example: Adobe Fonts (Typekit) - paste your kit script here
        <script>
            (function(d) {var config = { kitId: 'xxxxxx', scriptTimeout: 3000, async: true }, h=d.documentElement,t=setTimeout(function(){h.className=h.className.replace(/\bwf-loading\b/g,"")+" wf-inactive"},config.scriptTimeout),tk=d.createElement("script"),f=false,s=d.getElementsByTagName("script")[0],a;h.className += " wf-loading";tk.src='https://use.typekit.net/'+config.kitId+'.js';tk.async=true;tk.onload=tk.onreadystatechange=function(){a=this.readyState;if(f||a&&a!="complete"&&a!="loaded")return;f=true;clearTimeout(t);try{Typekit.load(config)}catch(e){}};s.parentNode.insertBefore(tk,s)})(document);
        </script>
        -->
        <link rel="shortcut icon" href="/images/favicon.ico" type="image/x-icon">
<link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <style>
        /* Add @font-face declarations for local webfont files. Place actual font files in `app/assets/fonts` */
        @font-face {
            font-family: 'Schibsted Grotesk';
            src: url('/assets/fonts/SchibstedGrotesk-Regular.woff2') format('woff2'),
                 url('/assets/fonts/SchibstedGrotesk-Regular.woff') format('woff');
            font-weight: 400;
            font-style: normal;
            font-display: swap;
        }

        @font-face {
            font-family: 'New Kansas';
            src: url('/assets/fonts/NewKansas-Regular.woff2') format('woff2'),
                 url('/assets/fonts/NewKansas-Regular.woff') format('woff');
            font-weight: 400;
            font-style: normal;
            font-display: swap;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-green: #00BA59;
            --dark-green: #005625;
            --highlight-green: #00D084;
            --light-bg: #FEFCF6;
            --dark-text: #515151;
            --gray-text: #666666;
            --white: #FFFFFF;
            --black: #07100C;
        }
  html{
    overflow-x: hidden;
  }
        body {
            font-family: 'Schibsted Grotesk', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: var(--white);
            color: var(--dark-text);
            line-height: 1.6;
            overflow-x: hidden;
        }
        .swiper{
          overflow: initial !important;
          max-width: 30%;
        }

        /* Navigation */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            padding: 20px 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
            box-shadow: 0px 4px 75px 0px #8d7c7c05;
backdrop-filter: blur(12px);
background: #FFFFFF8A;

        }

        .logo {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
        }
        .logo img {
            height: 36px;
            width: auto;
            display: block;
        }
        .logo .brand-text {
            font-size: 28px;
            font-weight: 700;
            color: var(--dark-text);
            font-family: serif;
        }

                .highlight{
                    font-family: 'New Kansas', serif;
                                font-weight: 600;
                }

        .nav-menu {
            display: flex;
            gap: 40px;
            list-style: none;
            align-items: center;
        }

        .nav-menu a {
            color: var(--dark-text);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
            font-size: 16px;
        }

        .nav-menu a:hover {
            color: var(--primary-green);
        }

        .cta-button {
            background: var(--dark-green);
            color: var(--white) !important;
            padding: 12px 32px;
            border-radius: 25px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            border: none;
            cursor: pointer;
            font-size: 16px;
        }

        .cta-button:hover {
            background: #0A6640;
            transform: translateY(-2px);
        }

        .menu-toggle {
            display: none;
            flex-direction: column;
            gap: 5px;
            cursor: pointer;
        }

        .menu-toggle span {
            width: 25px;
            height: 3px;
            background: var(--dark-text);
            transition: all 0.3s;
        }

        /* Hero Section */
        .hero {
            padding: 140px 5% 80px;
            background: var(--white);
            position:relative;
            z-index:0;
        }

        .hero-content {
            max-width: 700px;
        }

        .hero h1 {
            font-size: clamp(40px, 6vw, 60px);
            line-height: 1;
            margin-bottom: 24px;
            font-weight: 600;
            color: var(--dark-green);
        }

        .hero h1 .highlight {
            color: var(--primary-green);

        }

        .hero-image {
            margin: 60px 0;
            border-radius: 36px;
            overflow: hidden;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
        }

        .hero-image img {
            width: 100%;
            height: auto;
            display: block;
        }

        .logo-element{
            position:absolute;
            top:190px;
            right:150px;
            z-index:-1;
        }

        /* About Section */
        .about {
            padding: 80px 5%;
            background: var(--white);
        }

        .about-content {
            max-width: 800px;
        }

        .about h2 {
            font-size: clamp(32px, 5vw, 48px);

            line-height: 1;
            margin-bottom: 24px;
            color: var(--dark-text);
                        font-weight: 600;
            color: var(--dark-green);
        }

        .about h2 .highlight {
            color: var(--primary-green);
        }

        .about p {
            color: var(--gray-text);
            font-size: 18px;
            line-height: 1.7;
            margin-bottom: 40px;
        }

        /* Services Grid */
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 24px;
            margin-top: 60px;
        }

        .service-card {
            background: var(--black);
            color: var(--white);
            padding: 48px;
            border-radius: 24px;
            transition: all 0.3s;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .service-card.primary {
            background: var(--dark-green);
        }

        .service-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .service-card h3 {
            font-size: 32px;
            margin-bottom: 16px;
            font-weight: 700;
        }

        .service-card p {
            color: rgba(255, 255, 255, 0.8);
            line-height: 1.6;
            margin-bottom: 24px;
        }

        .service-link {
            display: inline-flex;
            align-items: center;
            gap: 12px;
            color: var(--white);
            text-decoration: none;
            font-weight: 600;
            transition: gap 0.3s;
        }

        .service-link:hover {
            gap: 16px;
        }

        .service-link::after {
            content: '→';
            font-size: 20px;
        }

        /* Portfolio Section */
        .portfolio {
            padding: 80px 5%;
            background: var(--white);
        }

        .section-header {
            text-align: center;
            margin-bottom: 60px;
        }

        .section-header h2 {
            font-size: clamp(32px, 5vw, 48px);

            margin-bottom: 16px;

                        font-weight: 600;
            color: var(--dark-green);
        }

        .section-header h2 .highlight {
            color: var(--primary-green);
        }

        .section-header p {
            color: var(--gray-text);
            font-size: 18px;
        }

        .portfolio-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 40px;
            margin-top: 60px;
        }

        .portfolio-item {
            cursor: pointer;
            transition: transform 0.3s;
        }

        .portfolio-item:hover {
            transform: translateY(-8px);
        }

        .portfolio-image {
            border-radius: 20px;
            overflow: hidden;
            margin-bottom: 20px;
            background: #FF5050;
            aspect-ratio: 1/1;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .portfolio-image.makiya {
            background: #FF5050;
        }

        .portfolio-image.uae {
            background: linear-gradient(135deg, #E8E8E8 0%, #D4D4D4 100%);
        }

        .portfolio-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .portfolio-title {
            font-size: 24px;
            font-weight: 600;
            color: var(--dark-text);
        }

        .portfolio-nav {
            display: flex;
            gap: 12px;
            justify-content: center;
            margin-top: 16px;
        }

        .portfolio-dot {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #D9D9D9;
            cursor: pointer;
            transition: all 0.3s;
        }

        .portfolio-dot.active {
            background: var(--dark-text);
            width: 32px;
            border-radius: 6px;
        }

        /* CTA Section */
        .cta-section {
            padding: 100px 5%;
background: #FEFCF6;
            text-align: center;
        }

        .cta-section h2 {
            font-size: clamp(28px, 4vw, 42px);
            font-weight: 700;
            line-height: 1.4;
            margin-bottom: 40px;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
            color: var(--dark-text);

                        font-weight: 600;
            color: var(--dark-green);
        }

        .cta-section h2 .highlight {
            color: var(--primary-green);
        }

        .cta-link {
            display: inline-flex;
            align-items: center;
            gap: 16px;
            font-size: 54px;
            font-weight: 500;
            color: var(--dark-green);
            text-decoration: none;
            transition: gap 0.3s;
                    font-family: 'New Kansas', serif;
        }

        .cta-link:hover {
            gap: 24px;
        }

        .cta-link::before {
            content: '→';
            font-size: 40px;
        }

        /* Contact Section */
        .contact {
            padding: 80px 5%;
            background: var(--light-bg);
        }

        .contact-wrapper {
            max-width: 800px;
            margin: 0 auto;
        }

        .contact h2 {
            font-size: clamp(36px, 5vw, 56px);
            font-weight: 600;
            margin-bottom: 16px;
            color: var(--dark-green);
        }

        .contact h2 .highlight {
            color: var(--primary-green);
        }

        .contact-intro {
            color: var(--gray-text);
            font-size: 18px;
            line-height: 1.7;
            margin-bottom: 48px;
        }

        .form-group {
            margin-bottom: 24px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 24px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--dark-text);
            font-size: 14px;
        }

        .required {
            color: #FF5050;
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 16px 20px;
            background: var(--white);
            border: 1px solid #D9D9D9;
            border-radius: 8px;
            color: var(--dark-text);
            font-size: 15px;
            transition: all 0.3s;
            font-family: inherit;
        }

        .form-group input::placeholder,
        .form-group textarea::placeholder {
            color: #999999;
        }

        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-green);
            background: var(--white);
        }

        .form-group select {
            cursor: pointer;
            appearance: none;
            background-image: url("data:image/svg+xml,%3Csvg width='12' height='8' viewBox='0 0 12 8' fill='none' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M1 1L6 6L11 1' stroke='%231A1A1A' stroke-width='2' stroke-linecap='round'/%3E%3C/svg%3E");
            background-repeat: no-repeat;
            background-position: right 20px center;
            padding-right: 50px;
        }

        .form-group textarea {
            resize: vertical;
            min-height: 120px;
        }

        .checkbox-group {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 16px;
            margin-top: 8px;
        }

        .checkbox-item {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .checkbox-item input[type="checkbox"] {
            width: 20px;
            height: 20px;
            cursor: pointer;
            accent-color: var(--primary-green);
        }

        .checkbox-item label {
            margin: 0;
            cursor: pointer;
            font-size: 15px;
            font-weight: 500;
        }

        .form-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: 40px;
        }

        .recaptcha-text {
            color: var(--gray-text);
            font-size: 14px;
        }

        .submit-button {
            padding: 16px 48px;
            background: var(--primary-green);
            color: var(--white);
            border: none;
            border-radius: 25px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .submit-button:hover {
            background: #0A6640;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(13, 124, 78, 0.3);
        }

        .submit-button:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }

        .form-message {
            margin-top: 20px;
            padding: 16px;
            border-radius: 8px;
            text-align: center;
            font-weight: 500;
        }

        .form-message.success {
            background: rgba(0, 208, 132, 0.1);
            color: var(--primary-green);
            border: 1px solid var(--primary-green);
        }

        .form-message.error {
            background: rgba(255, 80, 80, 0.1);
            color: #FF5050;
            border: 1px solid #FF5050;
        }

        /* Footer */
        .footer {
            padding: 60px 5% 40px;
            background: #FCF8E7;
            color: var(--black);
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 40px;
        }

        .footer-brand {
            max-width: 300px;
        }

        .footer-logo {
            margin-bottom: 24px;
        }
        .footer-logo img {
            height: 48px;
            width: auto;
            display: block;
        }

        .footer-info p  {
color: var(--black);
            font-size: 15px;
            line-height: 1.8;
        }

        .footer-social {
            text-align: left;
        }

        .footer-social h4 {
            font-size: 18px;
            margin-bottom: 16px;
            color: var(--dark-green)
        }

        .social-links {
            display: flex;
            gap: 12px;
            justify-content: flex-end;
                flex-direction: column;
        }

        .social-link, .footer-info a {
            color: rgba(255, 255, 255, 0.7);
color: var(--black);
            text-decoration: none;
            transition: color 0.3s;
            display: flex;
            align-items: center;
            gap: 6px;
            font-size: 15px;
        }

        .social-link svg {
            width: 16px;
            height: 16px;
            display: inline-block;
            vertical-align: middle;
            margin-right: 6px;
            flex: 0 0 auto;
        }

        .social-link:hover {
color: var(--black);
        }

        .footer-bottom {
            text-align: center;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: rgba(255, 255, 255, 0.5);
            color: var(--black);
            font-size: 14px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .navbar {
                padding: 15px 5%;
            }
                    .swiper-slide {
  width: 300px !important; /* Fixed width */
}
                    .swiper{
          max-width: 50%;
        }

            .nav-menu {
                position: fixed;
                top: 70px;
                left: 0;
                right: 0;
                background: var(--white);
                flex-direction: column;
                padding: 30px;
                gap: 20px;
                transform: translateX(100%);
                transition: transform 0.3s;
                border-top: 1px solid rgba(0, 0, 0, 0.05);
                box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            }

            .nav-menu.active {
                transform: translateX(0);
            }

            .menu-toggle {
                display: flex;
            }

            .menu-toggle.active span:nth-child(1) {
                transform: rotate(45deg) translate(6px, 6px);
            }

            .menu-toggle.active span:nth-child(2) {
                opacity: 0;
            }

            .menu-toggle.active span:nth-child(3) {
                transform: rotate(-45deg) translate(6px, -6px);
            }

            .hero {
                padding-top: 100px;
            }

            .form-row,
            .checkbox-group {
                grid-template-columns: 1fr;
            }

            .form-footer {
                flex-direction: column;
                gap: 20px;
                align-items: stretch;
            }

            .submit-button {
                width: 100%;
            }

            .footer-content {
                flex-direction: column;
                gap: 40px;
            }

            .footer-social {
                text-align: left;
            }

            .social-links {
                justify-content: flex-start;
                display: flex;
                flex-direction: column;
            }

            .portfolio-grid {
                grid-template-columns: 1fr;
            }

            .services-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 480px) {
                  .swiper{

          max-width: 50%;
        }   

           .hero {
            padding-bottom: 0px;
            }

  
            .hero h1 {
                font-size: 32px;
            }

            .service-card {
                padding: 32px;
            }

            .contact h2 {
                font-size: 32px;
            }
        }
    </style>
    <!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-PDY5Q72J9D"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-PDY5Q72J9D');
</script>
    <script src="https://www.google.com/recaptcha/api.js" async="" defer=""></script>
